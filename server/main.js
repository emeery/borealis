import { Meteor } from 'meteor/meteor';
import {Notas} from './../imports/api/notas';
Meteor.startup(() =>{
    console.log('donde estoy');
     /* Notas.insert({
		 nota: 'Una cuarta nota',
		fecha: new Date()
	}); */  
	/* Meteor.call('addNumbers') */
}); 

